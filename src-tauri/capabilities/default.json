{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for PixShrink.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
